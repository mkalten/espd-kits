# Presets

Optional example **`config.txt`** and starter patches per use case (class kit, installation, etc.).

These are **not** compiled into firmware — users copy them to SD or `/storage`, or sync with `espd/scripts/espd_sync.py` from a full `espd` checkout.
